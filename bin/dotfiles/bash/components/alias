# ALIAS

alias ll="ls -alrth"
alias ..="cd .."
alias ...="cd ../.."

take (){ 
	mkdir $1
	cd $1
}

# Open man page as PDF
function manpdf() {
 man -t "${1}" | open -f -a /Applications/Preview.app/
}
 
